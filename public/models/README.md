# BMW GLTF Models

Store your `.gltf` or `.glb` BMW model files in this folder.

Suggested naming:

- `bmw-5-series.gltf`
- `bmw-x5.gltf`
- `bmw-i4.gltf`

Later, these files can be loaded with Three.js from URLs like:

- `/models/bmw-5-series.gltf`
